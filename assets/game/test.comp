#version 430
layout(local_size_x = 1) in;

layout(std430, binding = 0) buffer out_layout {
    int size;
    int arr[];
};

void push_back(int a){
    arr[atomicAdd(size, 1)] = a;
}

void main(){
    if (gl_GlobalInvocationID.x % 2 == 0)
        push_back(int(gl_GlobalInvocationID.x));
}
