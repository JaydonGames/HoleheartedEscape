#version 430
layout(local_size_x = 1) in;

layout(std430, binding = 0) buffer out_layout {
    uint size;
    uint data[];
};

uint get_index(){
    return atomicAdd(size, 1);
}

void main(){
    uint index = get_index();
    data[index] = gl_GlobalInvocationID.x;
}
