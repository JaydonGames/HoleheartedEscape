#pragma once

namespace Render {
    struct Vec2 {
        int x, y;
    };

    struct Vec3 {
        int x, y, z;
    };

    struct Vec4 {
        int x, y, z, w;
    };

    struct Rect {
        float x, y;
        int w, h;
    };
}
